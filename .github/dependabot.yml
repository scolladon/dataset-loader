---
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "build"
    groups:
      github-actions:
        applies-to: version-updates
        patterns:
          - "*"
      github-actions-security:
        applies-to: security-updates
        patterns:
          - "*"

  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    versioning-strategy: increase
    commit-message:
      prefix: "build"
    allow:
      - dependency-type: "all"
    ignore:
      - dependency-name: "@types/node"
        update-types:
          - "version-update:semver-major"
    groups:
      runtime-dependencies:
        applies-to: version-updates
        dependency-type: "production"
        patterns:
          - "*"
      development-dependencies:
        applies-to: version-updates
        dependency-type: "development"
        patterns:
          - "*"
      # Security updates ignore version-update groups and would otherwise open one
      # ungrouped, off-schedule pull request each.
      runtime-dependencies-security:
        applies-to: security-updates
        dependency-type: "production"
        patterns:
          - "*"
      development-dependencies-security:
        applies-to: security-updates
        dependency-type: "development"
        patterns:
          - "*"
